apiVersion: v1
kind: Service
metadata:
  name: pgpool
  namespace: myproject
  labels:
    cluster-name: postgresql-cluster
    node-name: pgpool
spec:
  selector:
    cluster-name: postgresql-cluster
    node-name: pgpool
  ports:
  - port: 9999
    protocol: TCP
    targetPort: 9999
