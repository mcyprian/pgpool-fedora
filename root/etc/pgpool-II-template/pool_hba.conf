"local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
# IPv6 local connections:
host    all             all             ::1/128                 trust
# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            trust
host    replication     all             ::1/128                 trust
#
# Custom OpenShift repmgr configuration starting at this point.
#
local replication  repmgr     trust
host  replication  repmgr all scram-sha-256 

local repmgr       repmgr     trust
host  repmgr       repmgr all scram-sha-256

#
# Custom OpenShift configuration starting at this point.
#

# Allow connections from all hosts.
host all all all scram-sha-256

# Allow replication connections from all hosts.
host replication all all scram-sha-256

